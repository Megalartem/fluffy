export type AppSettings = {
  id: string; // e.g. "settings_ws_local"
  workspaceId: string; // "ws_local"
  defaultCurrency: string; // e.g. "VND"
  locale: string; // e.g. "ru-RU"
  createdAt: string; // ISO datetime
  updatedAt: string; // ISO datetime
};
