# ✅ Чек-лист качества кода для Fluffy

**Версия:** 1.0  
**Дата:** 23 декабря 2025  
**Для использования:** При PR review и в конце каждой фазы

---

## 📋 ЧЕК-ЛИСТЫ ПО ФАЗАМ

### ФАЗА 1: ФУНДАМЕНТ ✓ После завершения проверьте:

#### 1. DI Контейнер (1.1)
- [ ] `src/shared/di/container.ts` создан
- [ ] Метод `register<T>(key, factory, options)` работает
- [ ] Метод `get<T>(key)` работает с singleton
- [ ] Все Services зарегистрированы в контейнере
- [ ] Все Repos зарегистрированы в контейнере
- [ ] `setupDIContainer()` вызывается в app-shell
- [ ] Нет `new ServiceName()` в компонентах (только в контейнере)
- [ ] Unit-тесты для container написаны
- [ ] Нет дублирования кода при регистрации

**Критерии успеха:**
```typescript
// ✅ Правильно
const service = container.get<TransactionService>('TransactionService');

// ❌ Неправильно
const service = new TransactionService(...);
```

---

#### 2. Константный слой (1.2)
- [ ] `src/shared/constants/meta-keys.ts` создан
- [ ] `src/shared/constants/defaults.ts` создан
- [ ] `src/shared/constants/limits.ts` создан
- [ ] `src/shared/constants/index.ts` создан (экспорт)
- [ ] Все magic values заменены на константы
- [ ] Константы типизированы (используются `as const`)
- [ ] Каждая константа имеет JSDoc комментарий
- [ ] `"ws_local"` везде заменен на `LOCAL_WORKSPACE_ID`
- [ ] `"last_transaction_defaults"` везде заменен на `META_KEYS.LAST_TRANSACTION_DEFAULTS`

**Критерии успеха:**
```typescript
// ✅ Правильно
import { DEFAULTS, META_KEYS } from '@/shared/constants';
const key = META_KEYS.BUDGET_NOTIFIED(workspaceId, month);

// ❌ Неправильно
const key = `budget_notified_${workspaceId}_${month}`;
```

---

#### 3. WorkspaceContext (1.3)
- [ ] `src/shared/config/workspace-context.tsx` создан
- [ ] `WorkspaceProvider` обёртывает app-shell
- [ ] `useWorkspace()` хук работает корректно
- [ ] Выброс ошибки, если useWorkspace() используется вне Provider
- [ ] Все 30+ вызовов `new WorkspaceService().getCurrentWorkspaceId()` заменены на `useWorkspace().workspaceId`
- [ ] Компоненты с `useWorkspace()` отмечены как `"use client"`
- [ ] Unit-тесты написаны
- [ ] Нет новых `WorkspaceService()` вызовов в коде

**Критерии успеха:**
```typescript
// ✅ Правильно
const { workspaceId } = useWorkspace();

// ❌ Неправильно
const workspaceId = await new WorkspaceService().getCurrentWorkspaceId();
```

---

#### 4. Абстрактные интерфейсы (1.4)
- [ ] `src/core/repositories/` интерфейсы созданы
- [ ] `ITransactionsRepository` определен
- [ ] `ICategoriesRepository` определен
- [ ] `IBudgetsRepository` определен
- [ ] `IGoalsRepository` определен
- [ ] `ISettingsRepository` определен
- [ ] Все Dexie репо реализуют интерфейсы (`implements`)
- [ ] Services используют интерфейсы (не конкретные классы)
- [ ] In-Memory репо созданы для тестирования
- [ ] Unit-тесты используют In-Memory репо
- [ ] Нет прямых импортов `DexieXxxRepo` в Services (только интерфейсы)

**Критерии успеха:**
```typescript
// ✅ Правильно
export class TransactionService {
  constructor(private repo: ITransactionsRepository) {}
}

// ❌ Неправильно
export class TransactionService {
  constructor(private repo: DexieTransactionsRepo) {}
}
```

---

### ФАЗА 2: КОНСОЛИДАЦИЯ ✓ После завершения проверьте:

#### 5. MetaRegistry (2.1)
- [ ] `src/infra/notifications/meta-registry.ts` создан
- [ ] Все meta-ключи из `META_KEYS` используют registry
- [ ] Метод `registerKey()` добавляет новый ключ
- [ ] Метод `getKey()` возвращает значение
- [ ] Метод `setKey()` сохраняет значение
- [ ] Метод `deleteKey()` удаляет значение
- [ ] NotificationsService использует registry
- [ ] Дублирование meta-логики убрано
- [ ] Unit-тесты написаны

---

#### 6. App State Management (2.2)
- [ ] `src/shared/hooks/use-app-state.ts` создан
- [ ] `AppState` интерфейс определен
- [ ] `appStateReducer` функция работает
- [ ] `useAppState()` хук возвращает state и dispatch
- [ ] Старые хуки (`useDashboardData`, `useCategories` и т.д.) постепенно мигрируют
- [ ] Нет race conditions в состоянии
- [ ] Состояние синхронизировано между компонентами
- [ ] Unit-тесты для reducer написаны
- [ ] Нет дублирования логики загрузки данных

**Критерии успеха:**
```typescript
// ✅ Правильно (вместо множества отдельных состояний)
const { state, setTransactions, setCategories } = useAppState();

// ❌ Было раньше (дублирование)
const [txs, setTxs] = useState([]);
const [cats, setCats] = useState([]);
// ... в разных местах кода
```

---

#### 7. Error Boundary (2.3)
- [ ] `src/shared/ui/error-boundary.tsx` создан
- [ ] Error Boundary обёртывает основной контент в app-shell
- [ ] Fallback UI отображается при ошибке
- [ ] Ошибка логируется
- [ ] Кнопка "Перезагрузить" работает
- [ ] `AppError` обрабатывается с выводом кода ошибки
- [ ] Unit-тесты написаны
- [ ] Нет необработанных ошибок в консоли

---

#### 8. Валидаторы (2.4)
- [ ] `src/core/domain/validators/` созданы
- [ ] `TransactionValidator.validateCreate()` проверяет amount, type
- [ ] `TransactionValidator.validateUpdate()` проверяет изменения
- [ ] `CategoryValidator` создан
- [ ] `BudgetValidator` создан
- [ ] `GoalValidator` создан
- [ ] Services вызывают валидаторы перед сохранением
- [ ] Валидация выбросит `AppError` с деталями
- [ ] Unit-тесты для валидаторов написаны
- [ ] Нет возможности создать невалидную сущность

**Критерии успеха:**
```typescript
// ✅ Правильно
TransactionValidator.validateCreate(input);
const tx = await this.repo.create(workspaceId, ...);

// ❌ Неправильно
if (input.amount > 0) { // Валидация во многих местах
  const tx = await this.repo.create(workspaceId, ...);
}
```

---

### ФАЗА 3: ОПТИМИЗАЦИЯ ✓ После завершения проверьте:

#### 9. Логирование (3.1)
- [ ] `src/infra/logger/logger.ts` создан
- [ ] Logger имеет методы debug(), info(), warn(), error()
- [ ] Уровни логирования считываются из env переменной
- [ ] БД операции логируются (create, read, update, delete)
- [ ] Ошибки логируются в catch блоках
- [ ] Важные события логируются (импорт, экспорт)
- [ ] Логи не содержат чувствительные данные
- [ ] Unit-тесты для logger написаны
- [ ] Нет console.log() прямых вызовов (используется logger)

---

#### 10. TransactionSheet Рефакторинг (3.2)
- [ ] `transaction-sheet.tsx` разбита на компоненты:
  - [ ] `TransactionForm.tsx`
  - [ ] `TransactionPresets.tsx` (опционально)
  - [ ] `TransactionDeleteConfirm.tsx`
- [ ] Использование composition вместо одного большого компонента
- [ ] `use-transaction-form.ts` хук создан
- [ ] Логика формы вынесена в хук
- [ ] Валидация работает
- [ ] Сохранение defaults работает
- [ ] Файл `transaction-sheet.tsx` теперь < 150 строк
- [ ] Unit-тесты для формы написаны
- [ ] Разбиение не усложнило код

---

#### 11. Пагинация (3.3)
- [ ] `src/shared/hooks/use-paginated-list.ts` создан
- [ ] Infinite scroll реализован
- [ ] Результаты кешируются
- [ ] Фильтрация работает
- [ ] Сортировка работает
- [ ] List компоненты используют use-paginated-list
- [ ] Производительность улучшена для больших списков
- [ ] Unit-тесты написаны
- [ ] Нет дублирования пагинации кода

---

#### 12. Design System (3.4)
- [ ] Базовые компоненты созданы:
  - [ ] Button.tsx с вариантами (primary, secondary, danger)
  - [ ] Input.tsx с типами (text, number, date)
  - [ ] Card.tsx с padding и тенью
  - [ ] Badge.tsx с цветами
  - [ ] Select.tsx (опционально)
- [ ] Компоненты переиспользуются везде
- [ ] Нет inline styles (используются классы Tailwind)
- [ ] Пропсы типизированы
- [ ] Unit-тесты написаны
- [ ] Стили консистентны
- [ ] Accessibility (a11y) учтена

---

### ФАЗА 4: МАСШТАБИРУЕМОСТЬ ✓ После завершения проверьте:

#### 13. Cloud-Sync Подготовка (4.1)
- [ ] Модели обновлены с полями `syncStatus`, `version`, `lastSyncedAt`
- [ ] Миграция Dexie (version 6) создана
- [ ] `src/core/sync/sync-adapter.ts` интерфейс создан
- [ ] Все entity типы имеют sync-поля
- [ ] Database миграции работают без потери данных
- [ ] Unit-тесты для новых полей написаны

---

#### 14. Sync Инфраструктура (4.2)
- [ ] `src/infra/sync/` модуль создан
- [ ] `SyncQueue` класс создан (очередь операций)
- [ ] `ConflictResolver` создан (разрешение конфликтов)
- [ ] `SyncManager` создан (оркестрация)
- [ ] Retry механизм реализован
- [ ] Offline mode поддерживается
- [ ] Unit-тесты написаны

---

#### 15. Оптимизация БД (4.3)
- [ ] Индексы проанализированы
- [ ] Compound индексы добавлены где нужно
- [ ] Range queries используют индексы правильно
- [ ] Производительность улучшена (< 100ms для списков)
- [ ] Нет N+1 queries проблем
- [ ] Unit-тесты производительности написаны

---

#### 16. Документация (4.4)
- [ ] `docs/ARCHITECTURE.md` создан (архитектура)
- [ ] `docs/DEVELOPMENT.md` создан (guide для разработчиков)
- [ ] `docs/DATABASE.md` создан (schema и миграции)
- [ ] `docs/TESTING.md` создан (стратегия тестирования)
- [ ] `docs/CONTRIBUTING.md` создан (для контрибьюторов)
- [ ] README.md обновлен (убран шаблон Next.js)
- [ ] Все файлы содержат примеры
- [ ] Документация полная и актуальная

---

## 🧪 ОБЩИЕ КРИТЕРИИ КАЧЕСТВА

### Code Quality

- [ ] TypeScript strict mode включен
- [ ] Нет `any` типов (используются правильные типы)
- [ ] Nesting глубины < 3 уровней
- [ ] Функции < 20 строк (в среднем)
- [ ] Файлы < 300 строк (в большинстве)
- [ ] Нет дублирования кода (DRY принцип)
- [ ] Переменные имеют понятные имена
- [ ] Функции делают одно (SRP принцип)
- [ ] Используются const по умолчанию

### Performance

- [ ] Список загружается за < 200ms
- [ ] Фильтрация без lag
- [ ] Сортировка без lag
- [ ] No memory leaks (cleanup в useEffect)
- [ ] Efficient re-renders (React.memo где нужно)
- [ ] Indexed queries в Dexie

### Testing

- [ ] Unit тесты для всех Services (> 80% coverage)
- [ ] Unit тесты для всех Validators
- [ ] Unit тесты для Hooks
- [ ] Unit тесты для DI Container
- [ ] Integration тесты для основных flows
- [ ] Тесты запускаются без ошибок
- [ ] Coverage > 70% для критичного кода

### Security

- [ ] Нет SQL injection (используется Dexie, не SQL)
- [ ] Нет XSS (React escapes по умолчанию)
- [ ] Нет sensitive data в логах
- [ ] Input валидируется везде
- [ ] Output санитизируется где нужно

### Accessibility (a11y)

- [ ] Кнопки имеют label или aria-label
- [ ] Input имеют label или aria-label
- [ ] Цвета контрастные (WCAG AA)
- [ ] Keyboard navigation работает
- [ ] Screen readers могут читать контент

---

## 📝 ШАБЛОН ДЛЯ PR REVIEW

При каждом PR перед мержем проверьте:

```markdown
## Чек-лист для Review

**Задача:** [номер из TODO]  
**Описание:** Краткое описание  

### Code Quality
- [ ] TypeScript типы правильные
- [ ] Нет дублирования кода
- [ ] Функции < 20 строк
- [ ] Нет console.log() и debugger

### Testing
- [ ] Unit тесты добавлены
- [ ] Coverage > 70%
- [ ] Тесты проходят

### Architecture
- [ ] Следует плану рефакторинга
- [ ] Нет нарушения принципов (SOLID, DRY)
- [ ] Интерфейсы используются (не конкретные классы)

### Документация
- [ ] JSDoc где нужно
- [ ] Комментарии для сложной логики
- [ ] Updated REFACTORING_TODO.md

### Performance
- [ ] Нет N+1 queries
- [ ] No memory leaks
- [ ] Efficient re-renders

### Security
- [ ] Input валидируется
- [ ] Нет sensitive data в логах
```

---

## 🎯 КРИТЕРИИ УСПЕХА ПО ФАЗАМ

### ФАЗА 1 — Успех, если:
✅ DI контейнер полностью работает  
✅ Константы везде используются  
✅ WorkspaceContext убрал все `new WorkspaceService()`  
✅ Все Services используют интерфейсы Repos  
✅ Нет регрессии в функционале  
✅ Unit-тесты для новых компонентов написаны  

### ФАЗА 2 — Успех, если:
✅ Единое управление состоянием работает  
✅ Ошибки обрабатываются везде  
✅ Валидация работает для всех операций  
✅ Meta-ключи управляются централизованно  
✅ Нет необработанных ошибок в консоли  

### ФАЗА 3 — Успех, если:
✅ Большие компоненты разбиты на smaller pieces  
✅ Design System используется везде  
✅ Списки работают с пагинацией  
✅ Логирование работает  
✅ Производительность улучшена  

### ФАЗА 4 — Успех, если:
✅ Модели готовы к cloud-sync  
✅ Документация полная  
✅ Есть тесты для новой инфраструктуры  
✅ Проект готов к масштабированию  

---

## ⚠️ RED FLAGS (Стоп-сигналы)

Если видите эти проблемы, остановитесь и перепроверьте:

- ❌ Новые `new ServiceName()` вызовы (должны быть в DI только)
- ❌ Новые magic strings вместо констант
- ❌ Проверка `instanceof` вместо интерфейсов
- ❌ Прямые импорты `DexieXxxRepo` в компонентах
- ❌ Дублирование логики работы с meta-ключами
- ❌ Большие компоненты (> 300 строк)
- ❌ Тесты не написаны
- ❌ Coverage упал (< 60%)
- ❌ Performance упала (list load > 500ms)
- ❌ Регрессия в функционале

---

## 📊 МЕТРИКИ УСПЕХА

После завершения всех фаз должно быть:

```
✅ Тестируемость:       Unit test coverage > 75%
✅ Архитектура:         0 нарушений SOLID принципов
✅ Производительность:  List load < 200ms, Filter < 100ms
✅ Документация:        > 80% документировано (JSDoc)
✅ Code Quality:        ESLint errors = 0
✅ Security:            Security audit passed
✅ Accessibility:       WCAG AA compliant
```

---

**Версия:** 1.0  
**Статус:** Ready to use  
**Последнее обновление:** 23 декабря 2025
